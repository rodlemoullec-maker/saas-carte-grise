# Imatra — version locale (logiciel installé chez l'agent)
# Toutes les dépendances cloud (Twilio, Stripe, Google DocAI, AWS) ont été retirées

# ─── Framework web ──────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ─── Base de données SQLite locale ──────────────────────────
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# ─── Configuration ───────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ─── OCR local — PaddleOCR ───────────────────────────────────
paddleocr>=2.7.0
paddlepaddle>=2.6.0
pillow>=10.3.0
pdf2image>=1.17.0

# ─── Parsing emails (drag & drop) ────────────────────────────
extract-msg>=0.48.0  # Pour les emails Outlook .msg
# Le module 'email' standard de Python gère .eml

# ─── Génération de PDF Cerfa ─────────────────────────────────
fpdf2>=2.7.0
pypdf>=3.17.0

# ─── Vérifications croisées et fuzzy matching ────────────────
python-dateutil>=2.9.0
fuzzywuzzy>=0.18.0
python-levenshtein>=0.25.0

# ─── Stockage local chiffré ──────────────────────────────────
cryptography>=42.0.0

# ─── HTTP client (pour licence et MAJ règles uniquement) ─────
httpx>=0.27.0

# ─── Utilitaires ─────────────────────────────────────────────
aiofiles>=23.2.0
